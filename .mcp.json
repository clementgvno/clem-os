{
  "mcpServers": {
    "convex": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "convex@latest",
        "mcp",
        "start",
        "--project-dir",
        "${CLAUDE_PROJECT_DIR:-.}"
      ],
      "env": {}
    }
  }
}
